APP_ENV=local
PORT=8080


POSTGRES_USER=user
POSTGRES_PASSWORD=
POSTGRES_DB=training
DATABASE_URL=postgres://user:PASSWORD@localhost:5433/training?sslmode=disable&connect_timeout=5

# openssl rand -base64 48
JWT_SECRET=
JWT_TTL=30m

LOG_LEVEL=info
LOG_FORMAT=text

DOMAIN=training.example.com
